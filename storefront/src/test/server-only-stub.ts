/**
 * Празен заместител на пакета `server-only` за Vitest.
 *
 * В Next-а той нарочно чупи build-а, ако сървърен модул попадне в клиентски
 * бъндъл. В unit тестовете няма бъндъл, така че тук е no-op.
 */
export {};
